import styled from 'styled-components';

export const IconWrapper = styled.button`
    border: unset;
    background: unset;
    padding: unset;
    margin: 0 2px;
    display: flex;
    align-items: center;
    color: grey;
    cursor: pointer;
`;